<mongoChangeLog>
    <changeSet changeId="base-active-field-migration" author="anubhawsrivastava">
        <script>
            db.getCollectionNames().forEach(function (collname) {
                if (collname != "system.profile") {
                    db[collname].deleteMany({active: false});
                    db[collname].updateMany({}, {$unset: { active: ""}});
                }
            });
        </script>
    </changeSet>
</mongoChangeLog>
