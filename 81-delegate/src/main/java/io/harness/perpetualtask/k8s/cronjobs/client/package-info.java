/**
 * Contains a minimal client for querying CronJob objects as fabric8 version we use don't support CJs.
 * Added as a workaround, and to be removed once we upgrade fabric8.
 */
package io.harness.perpetualtask.k8s.cronjobs.client;
