load("@rules_pkg//:pkg.bzl", "pkg_tar")

pkg_tar(
    name = "watcher_jar",
    srcs = ["watcherlocal.txt"],
    files = {
        "//250-watcher:module_deploy.jar": "watcher.jar",
    },
    mode = "0644",
    package_dir = "/local-storage/wingswatchers",
    tags = [
        "manual",
        "no-cache",
        "no-ide",
    ],
    visibility = ["//visibility:public"],
)
