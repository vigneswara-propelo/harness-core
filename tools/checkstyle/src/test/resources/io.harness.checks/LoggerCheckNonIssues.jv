package io.harness.checks;

public class LoggerCheckNonCheckIssues {
  private static final Logger logger = LoggerFactory.getLogger(LoggerCheckNonCheckIssues.class);
  public abstract Logger getLogger();
  Logger mockLogger = mock(Logger.class);
  Logger logger = mock(Logger.class);
}
