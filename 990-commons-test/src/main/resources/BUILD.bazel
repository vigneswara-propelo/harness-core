package(default_visibility = ["//visibility:public"])

load("//:macros.bzl", "resources")

filegroup(
    name = "resource",
    srcs = [
        "logback-test.xml",
        "secrets.properties",
    ],
)

resources()
