interface Outcome {
  execution: Execution
}

type OutcomeConnection {
  pageInfo: PageInfo
  nodes: [Outcome]
}

type DeploymentOutcome implements Outcome {
  execution: Execution
  service: Service
}
