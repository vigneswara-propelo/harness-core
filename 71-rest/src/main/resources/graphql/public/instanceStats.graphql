enum InstanceAggregation {
  Application
  Service
  Environment
  CloudProvider
  InstanceType
}

extend type Query {
  # Get instance stats.
  instanceStats(filters: [InstanceFilter], groupBy: [InstanceAggregation], groupByTime: TimeSeriesAggregation): Data
    @dataFetcher(name: instanceStats)
}
