input ApplicationAggregation {
  tagAggregation: TagAggregation
}

extend type Query {
  #Get statistics for a Harness Application.
  applicationStats(groupBy: [ApplicationAggregation]): SinglePointData @dataFetcher(name: applicationStats)
}
