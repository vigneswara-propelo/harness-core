summary: "Harness SRM. The Service Health for service ${SERVICE_NAME} in environment ${ENVIRONMENT_NAME}  has breached a condition. AccountID ${ACCOUNT_ID} OrganisationID ${ORG_ID} ProjectID ${PROJECT_ID} Rule ${RULE_NAME} Condition ${CONDITION_NAME}. Incident occurred ${START_DATE}. Click URL to goto Service Health dashboard ${URL}"
link:
    href: harness.io
    text: Harness