name: Task count metrics
identifier: task_count_metrics
metricGroup: account
metrics:
  - metricName: verification_job_instance_timeout_count
    metricDefinition: verification job instance timeout count
    type: Count
    unit: "1"
