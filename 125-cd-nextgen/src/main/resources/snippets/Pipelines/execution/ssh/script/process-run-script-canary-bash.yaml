# i=0
                              # while [ "$i" -lt 30 ]
                              # do
                              #     pgrep -f "\-Dcatalina.home=$HARNESS_RUNTIME_PATH/tomcat"
                              #     rc=$?
                              #     if [ "$rc" -eq 0 ]
                              #     then
                              #         exit 0
                              #         sleep 1
                              #         i=$((i+1))
                              #     else
                              #         sleep 1
                              #         i=$((i+1))
                              #     fi
                              # done
                              # exit 1