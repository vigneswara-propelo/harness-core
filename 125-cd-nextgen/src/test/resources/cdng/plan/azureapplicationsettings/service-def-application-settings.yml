serviceDefinition:
  spec:
    applicationSettings:
      store:
        type: Github
        spec:
          connectorRef: gitConnector
          gitFetchType: Branch
          paths:
            - azure_webapp/application-settings-1.json
          branch: main
  type: AzureWebApp
  gitOpsEnabled: false