inputSet:
  identifier: wrongInput2
  name: wrongInput2
  pipeline:
    identifier: myPipeline1
    stages:
      - stage:
          identifier: prod
          type: Deployment
          spec:
            service:
              useFromStage:
                stage: new-qa
              stageOverrides:
                manifests:   # {k8s |  values | pcf |  helmSourceRepo | helmSourceRepo | kustomize | openShift}
                  - manifest:
                      identifier: prodOverride
                      type: Values
                      spec:
                        store:
                          type: Git
                          spec:
                            connectorRef: <+input>
                            gitFetchType: Commit