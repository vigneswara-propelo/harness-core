# to be used when building in Jenkins
FROM harness/serverjre_8:242

# Add the capsule JAR and config.yml
COPY command-library-app-capsule.jar AppServerAgent-4.5.0.23604.tar.gz keystore.jks command-library-server-config.yml /opt/harness/

COPY scripts /opt/harness

RUN chmod +x /opt/harness/*.sh
RUN mkdir -p /opt/harness/plugins

WORKDIR /opt/harness

CMD [ "./run.sh" ]
