collection(DelegateGroup)
    .filter(accountId == @, uuid == @)
collection(DelegateTask)
    .filter(accountId == @, uuid == @)
collection(DelegateTask)
    .filter(accountId == @, uuid in [@], delegateId == @, status == @)
    .project(uuid, data_timeout)
collection(DelegateTask)
    .filter(status == @, expiry < @)
collection(DelegateTask)
    .filter(status == @, expiry < @, createdAt % @divisor == @remainder)