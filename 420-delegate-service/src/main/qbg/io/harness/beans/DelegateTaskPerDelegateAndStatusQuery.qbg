query PerDelegateAndStatus for io.harness.beans.DelegateTask {
  filter accountId as String;
  filter uuid as String from list;
  filter delegateId as String;
  filter status as io.harness.beans.DelegateTask.Status;

  project uuid;
  project data_timeout;
}