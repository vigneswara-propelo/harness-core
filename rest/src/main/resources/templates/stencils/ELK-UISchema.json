{
  "ui:order": ["analysisServerConfigId", "query", "indices", "hostnameField", "messageField", "timeDuration", "comparisonStrategy", "analysisTolerance", "executeWithPreviousSteps"],
  "query": {
    "ui:widget": "textarea"
  }
}
