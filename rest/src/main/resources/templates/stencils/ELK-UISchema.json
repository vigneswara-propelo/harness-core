{
  "ui:order": ["analysisServerConfigId", "query", "indices", "hostnameField", "messageField", "timeDuration", "comparisonStrategy", "analysisTolerance"],
  "query": {
    "ui:widget": "textarea"
  }
}
