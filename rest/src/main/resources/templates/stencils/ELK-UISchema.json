{
  "ui:order": ["analysisServerConfigId", "query", "indices", "hostnameField", "messageField", "timestampFormat", "timeDuration", "comparisonStrategy", "analysisTolerance", "executeWithPreviousSteps"],
  "query": {
    "ui:widget": "textarea"
  }
}
