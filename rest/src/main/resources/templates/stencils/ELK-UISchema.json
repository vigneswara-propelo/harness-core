{
  "ui:order" :
      [
        "analysisServerConfigId", "query", "queryType", "indices",
        "hostnameField", "messageField", "timestampFormat", "timeDuration",
        "comparisonStrategy", "analysisTolerance", "executeWithPreviousSteps"
      ],
      "query" : {
    "ui:widget" : "textarea"
  }
}
