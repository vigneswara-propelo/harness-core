{
  "ui:order": ["analysisServerConfigId", "query", "indices", "hostnameField", "messageField", "timestampField", "timestampFieldFormat", "timeDuration", "comparisonStrategy"],
  "query": {
    "ui:widget": "textarea"
  }
}
