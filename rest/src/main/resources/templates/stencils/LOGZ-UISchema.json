{
  "ui:order": ["analysisServerConfigId", "query", "hostnameField", "messageField", "timestampField", "timestampFieldFormat", "timeDuration", "comparisonStrategy"],
  "query": {
    "ui:widget": "textarea"
  }
}
