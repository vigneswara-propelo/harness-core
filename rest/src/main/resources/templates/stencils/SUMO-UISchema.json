{
  "ui:order": ["analysisServerConfigId", "query", "hostnameField", "hostnameTemplate", "timeDuration", "comparisonStrategy", "analysisTolerance", "executeWithPreviousSteps", "autoBaseline"],
  "query": {
    "ui:widget": "textarea"
  },
  "hostnameTemplate": {
    "ui:placeholder": "This defaults to hostname"
  }
}