ENTITY_CREATE_NOTIFICATION:
      web: A new ${ENTITY_TYPE} ${ENTITY_NAME} is created.
      slack: A new ${ENTITY_TYPE} ${ENTITY_NAME} is created.
      email:
          subject: ${ENTITY_TYPE} is created
          body: A new ${ENTITY_TYPE} ${ENTITY_NAME} is created.

ENTITY_DELETE_NOTIFICATION:
      web: ${ENTITY_TYPE} ${ENTITY_NAME} is deleted.
      slack: ${ENTITY_TYPE} ${ENTITY_NAME} is deleted.
      email:
          subject: ${ENTITY_TYPE} is deleted
          body: ${ENTITY_TYPE} ${ENTITY_NAME} is deleted.

ARTIFACT_APPROVAL_NOTIFICATION:
      web: ${ENTITY_TYPE} ${ENTITY_NAME} is waiting for <${ACTION_URL}|approval>
      slack: ${ENTITY_TYPE} ${ENTITY_NAME} is waiting for <${ACTION_URL}|approval>
      email:
          subject: Artifact approval required
          body: ${ENTITY_TYPE} ${ENTITY_NAME} is waiting for <${ACTION_URL}|approval>

ARTIFACT_APPROVAL_NOTIFICATION_STATUS:
      web: ${ENTITY_TYPE} ${ENTITY_NAME} is <${ACTION_URL}|${NOTIFICATION_STATUS}> by ${USER_NAME}
      slack: ${ENTITY_TYPE} ${ENTITY_NAME} is <${ACTION_URL}|${NOTIFICATION_STATUS}> by ${USER_NAME}
      email:
          subject: Artifact status changed
          body: ${ENTITY_TYPE} ${ENTITY_NAME} is <${ACTION_URL}|${NOTIFICATION_STATUS}> by ${USER_NAME}

WORKFLOW_SUCCESSFUL_NOTIFICATION:
      web: ${WORKFLOW_NAME} completed successfully for ${ARTIFACTS} in ${ENV_NAME} at ${DATE}
      slack: ${WORKFLOW_NAME} completed successfully for ${ARTIFACTS} in ${ENV_NAME} at ${DATE}
      email:
          subject: ${WORKFLOW_NAME} completed successfully
          body: ${WORKFLOW_NAME} completed successfully for ${ARTIFACTS} in ${ENV_NAME} at ${DATE}

WORKFLOW_FAILED_NOTIFICATION:
      web: ${WORKFLOW_NAME} failed for ${ARTIFACTS} in ${ENV_NAME} at ${DATE}
      slack: ${WORKFLOW_NAME} failed for ${ARTIFACTS} in ${ENV_NAME} at ${DATE}
      email:
          subject: ${WORKFLOW_NAME} failed
          body: ${WORKFLOW_NAME} failed for ${ARTIFACTS} in ${ENV_NAME} at ${DATE}

WORKFLOW_PAUSED_NOTIFICATION:
      web: ${WORKFLOW_NAME} paused for ${ARTIFACTS} in ${ENV_NAME} at ${DATE}
      slack: ${WORKFLOW_NAME} paused for ${ARTIFACTS} in ${ENV_NAME} at ${DATE}
      email:
          subject: ${WORKFLOW_NAME} paused
          body: ${WORKFLOW_NAME} paused for ${ARTIFACTS} in ${ENV_NAME} at ${DATE}

WORKFLOW_RESUMED_NOTIFICATION:
      web: ${WORKFLOW_NAME} execution resumed for ${ARTIFACTS} in ${ENV_NAME} at ${DATE}
      slack: ${WORKFLOW_NAME} execution resumed for ${ARTIFACTS} in ${ENV_NAME} at ${DATE}
      email:
          subject: ${WORKFLOW_NAME} resumed
          body: ${WORKFLOW_NAME} resumed for ${ARTIFACTS} in ${ENV_NAME} at ${DATE}

WORKFLOW_ABORTED_NOTIFICATION:
      web: ${WORKFLOW_NAME} aborted for ${ARTIFACTS} in ${ENV_NAME} at ${DATE}
      slack: ${WORKFLOW_NAME} aborted for ${ARTIFACTS} in ${ENV_NAME} at ${DATE}
      email:
          subject: ${WORKFLOW_NAME} aborted
          body: ${WORKFLOW_NAME} aborted for ${ARTIFACTS} in ${ENV_NAME} at ${DATE}

WORKFLOW_PHASE_SUCCESSFUL_NOTIFICATION:
      web: ${PHASE_NAME} of ${WORKFLOW_NAME} completed successfully for ${ARTIFACTS} at ${DATE}
      slack: ${PHASE_NAME} of ${WORKFLOW_NAME} completed successfully for ${ARTIFACTS} at ${DATE}
      email:
          subject: ${PHASE_NAME} of ${WORKFLOW_NAME} completed
          body: ${PHASE_NAME} of ${WORKFLOW_NAME} completed successfully for ${ARTIFACTS} at ${DATE}

WORKFLOW_PHASE_FAILED_NOTIFICATION:
      web: ${PHASE_NAME} of ${WORKFLOW_NAME} failed for ${ARTIFACTS} at ${DATE}
      slack: ${PHASE_NAME} of ${WORKFLOW_NAME} failed for ${ARTIFACTS} at ${DATE}
      email:
          subject: ${PHASE_NAME} of ${WORKFLOW_NAME} failed
          body: ${PHASE_NAME} of ${WORKFLOW_NAME} failed for ${ARTIFACTS} at ${DATE}

WORKFLOW_PHASE_PAUSED_NOTIFICATION:
      web: ${PHASE_NAME} of ${WORKFLOW_NAME} paused for ${ARTIFACTS} at ${DATE}
      slack: ${PHASE_NAME} of ${WORKFLOW_NAME} paused for ${ARTIFACTS} at ${DATE}
      email:
          subject: ${PHASE_NAME} of ${WORKFLOW_NAME} paused
          body: ${PHASE_NAME} of ${WORKFLOW_NAME} paused for ${ARTIFACTS} at ${DATE}
