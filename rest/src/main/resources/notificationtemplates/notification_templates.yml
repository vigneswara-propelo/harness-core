ENTITY_CREATE_NOTIFICATION:
      web: A new ${ENTITY_TYPE} <b>${ENTITY_NAME}</b> is created.
      slack: A new ${ENTITY_TYPE} \*${ENTITY_NAME}\* is created.
      email:
          subject: ${ENTITY_TYPE} ${ENTITY_NAME} is created
          body: A new ${ENTITY_TYPE} <b>${ENTITY_NAME}</b> is created.

ENTITY_DELETE_NOTIFICATION:
      web: ${ENTITY_TYPE} <b>${ENTITY_NAME}</b> is deleted.
      slack: ${ENTITY_TYPE} \*${ENTITY_NAME}\* is deleted.
      email:
          subject: ${ENTITY_TYPE} ${ENTITY_NAME} is deleted
          body: ${ENTITY_TYPE} <b>${ENTITY_NAME}</b> is deleted.

ARTIFACT_APPROVAL_NOTIFICATION:
      web: ${ENTITY_TYPE} <b>${ENTITY_NAME}</b> is waiting for <a href="${ACTION_URL}" target="_blank">approval</a>
      slack: ${ENTITY_TYPE} \*${ENTITY_NAME}\* is waiting for <${ACTION_URL}|approval>
      email:
          subject: Artifact approval required for ${ENTITY_TYPE} ${ENTITY_NAME}
          body: ${ENTITY_TYPE} <b>${ENTITY_NAME}</b> is waiting for <a href="${ACTION_URL}" target="_blank">approval</a>

ARTIFACT_APPROVAL_NOTIFICATION_STATUS:
      web: ${ENTITY_TYPE} <b>${ENTITY_NAME}</b> is <a href="${ACTION_URL}" target="_blank">${NOTIFICATION_STATUS}</a> by <b>${USER_NAME}</b>
      slack: ${ENTITY_TYPE} \*${ENTITY_NAME}\* is <${ACTION_URL}|${NOTIFICATION_STATUS}> by \*${USER_NAME}\*${PIPELINE}
      email:
          subject: Artifact status changed for ${ENTITY_TYPE} ${ENTITY_NAME}
          body: ${ENTITY_TYPE} <b>${ENTITY_NAME}</b> is <a href="${ACTION_URL}" target="_blank">${NOTIFICATION_STATUS}</a> by <b>${USER_NAME}</b>

WORKFLOW_SUCCESSFUL_NOTIFICATION:
      web: <b><a href="${WORKFLOW_URL}" target="_blank">${WORKFLOW_NAME}</a></b> completed successfully for <b>${ARTIFACTS}</b> in ${ENV_NAME}, triggered by <b>${USER_NAME}</b>${PIPELINE}<br><br>Started ${START_DATE} and completed at at ${END_DATE}<br><i>${DURATION}</i>
      slack: \*<${WORKFLOW_URL}|${WORKFLOW_NAME}>\* completed successfully for \*${ARTIFACTS}\* in ${ENV_NAME}, triggered by \*${USER_NAME}\*${PIPELINE}||Started<!date^${START_TS_SECS}^ {date_short_pretty} at {time}| ${START_DATE}> and completed at <!date^${END_TS_SECS}^{time}|${END_DATE}>||_${DURATION}_||completed
      email:
          subject: ${WORKFLOW_NAME} completed successfully
          body: <b><a href="${WORKFLOW_URL}" target="_blank">${WORKFLOW_NAME}</a></b> completed successfully for <b>${ARTIFACTS}</b> in ${ENV_NAME}, triggered by <b>${USER_NAME}</b>${PIPELINE}<br><br>Started ${START_DATE} and completed at ${END_DATE}<br><i>${DURATION}</i>

WORKFLOW_FAILED_NOTIFICATION:
      web: <b><a href="${WORKFLOW_URL}" target="_blank">${WORKFLOW_NAME}</a></b> failed for <b>${ARTIFACTS}</b> in ${ENV_NAME}, triggered by <b>${USER_NAME}</b>${PIPELINE}<br><br>Started ${START_DATE} and failed at ${END_DATE}<br><i>${DURATION}</i>
      slack: \*<${WORKFLOW_URL}|${WORKFLOW_NAME}>\* failed for \*${ARTIFACTS}\* in ${ENV_NAME}, triggered by \*${USER_NAME}\*${PIPELINE}||Started<!date^${START_TS_SECS}^ {date_short_pretty} at {time}| ${START_DATE}> and failed at <!date^${END_TS_SECS}^{time}|${END_DATE}>||_${DURATION}_||failed
      email:
          subject: ${WORKFLOW_NAME} failed
          body: <b><a href="${WORKFLOW_URL}" target="_blank">${WORKFLOW_NAME}</a></b> failed for <b>${ARTIFACTS}</b> in ${ENV_NAME}, triggered by <b>${USER_NAME}</b>${PIPELINE}<br><br>Started ${START_DATE} and failed at ${END_DATE}<br><i>${DURATION}</i>

WORKFLOW_PAUSED_NOTIFICATION:
      web: <b><a href="${WORKFLOW_URL}" target="_blank">${WORKFLOW_NAME}</a></b> paused for <b>${ARTIFACTS}</b> in ${ENV_NAME}, triggered by <b>${USER_NAME}</b>${PIPELINE}<br><br>Started ${START_DATE} and paused at ${END_DATE}<br><i>${DURATION}</i>
      slack: \*<${WORKFLOW_URL}|${WORKFLOW_NAME}>\* paused for \*${ARTIFACTS}\* in ${ENV_NAME}, triggered by \*${USER_NAME}\*${PIPELINE}||Started<!date^${START_TS_SECS}^ {date_short_pretty} at {time}| ${START_DATE}> and paused at <!date^${END_TS_SECS}^{time}|${END_DATE}>||_${DURATION}_||paused
      email:
          subject: ${WORKFLOW_NAME} paused
          body: <b><a href="${WORKFLOW_URL}" target="_blank">${WORKFLOW_NAME}</a></b> paused for <b>${ARTIFACTS}</b> in ${ENV_NAME}, triggered by <b>${USER_NAME}</b>${PIPELINE}<br><br>Started ${START_DATE} and paused at ${END_DATE}<br><i>${DURATION}</i>

WORKFLOW_RESUMED_NOTIFICATION:
      web: <b><a href="${WORKFLOW_URL}" target="_blank">${WORKFLOW_NAME}</a></b> execution resumed for <b>${ARTIFACTS}</b> in ${ENV_NAME}, triggered by <b>${USER_NAME}</b>${PIPELINE}<br><br>Started ${START_DATE} and resumed at ${END_DATE}<br><i>${DURATION}</i>
      slack: \*<${WORKFLOW_URL}|${WORKFLOW_NAME}>\* execution resumed for \*${ARTIFACTS}\* in ${ENV_NAME}, triggered by \*${USER_NAME}\*${PIPELINE}||Started<!date^${START_TS_SECS}^ {date_short_pretty} at {time}| ${START_DATE}> and resumed at <!date^${END_TS_SECS}^{time}|${END_DATE}>||_${DURATION}_||resumed
      email:
          subject: ${WORKFLOW_NAME} resumed
          body: <b><a href="${WORKFLOW_URL}" target="_blank">${WORKFLOW_NAME}</a></b> resumed for <b>${ARTIFACTS}</b> in ${ENV_NAME}, triggered by <b>${USER_NAME}</b>${PIPELINE}<br><br>Started ${START_DATE} and resumed at ${END_DATE}<br><i>${DURATION}</i>

WORKFLOW_ABORTED_NOTIFICATION:
      web: <b><a href="${WORKFLOW_URL}" target="_blank">${WORKFLOW_NAME}</a></b> aborted for <b>${ARTIFACTS}</b> in ${ENV_NAME}, triggered by <b>${USER_NAME}</b>${PIPELINE}<br><br>Started ${START_DATE} and aborted at ${END_DATE}<br><i>${DURATION}</i>
      slack: \*<${WORKFLOW_URL}|${WORKFLOW_NAME}>\* aborted for \*${ARTIFACTS}\* in ${ENV_NAME}, triggered by \*${USER_NAME}\*${PIPELINE}||Started<!date^${START_TS_SECS}^ {date_short_pretty} at {time}| ${START_DATE}> and aborted at <!date^${END_TS_SECS}^{time}|${END_DATE}>||_${DURATION}_||aborted
      email:
          subject: ${WORKFLOW_NAME} aborted
          body: <b><a href="${WORKFLOW_URL}" target="_blank">${WORKFLOW_NAME}</a></b> aborted for <b>${ARTIFACTS}</b> in ${ENV_NAME}, triggered by <b>${USER_NAME}</b>${PIPELINE}<br><br>Started ${START_DATE} and aborted at ${END_DATE}<br><i>${DURATION}</i>

WORKFLOW_PHASE_SUCCESSFUL_NOTIFICATION:
      web: ${PHASE_NAME} of <b><a href="${WORKFLOW_URL}" target="_blank">${WORKFLOW_NAME}</a></b> completed successfully for <b>${ARTIFACTS}</b>, triggered by <b>${USER_NAME}</b>${PIPELINE}<br><br>Started ${START_DATE} and completed at ${END_DATE}<br><i>${DURATION}</i>
      slack: ${PHASE_NAME} of \*${WORKFLOW_NAME}\* completed successfully for \*${ARTIFACTS}\*, triggered by \*${USER_NAME}\*${PIPELINE}||Started<!date^${START_TS_SECS}^ {date_short_pretty} at {time}| ${START_DATE}> and completed at <!date^${END_TS_SECS}^{time}|${END_DATE}>||_${DURATION}_||completed
      email:
          subject: ${PHASE_NAME} of ${WORKFLOW_NAME} completed
          body: ${PHASE_NAME} of <b><a href="${WORKFLOW_URL}" target="_blank">${WORKFLOW_NAME}</a></b> completed successfully for <b>${ARTIFACTS}</b>, triggered by <b>${USER_NAME}</b>${PIPELINE}<br><br>Started ${START_DATE} and completed at ${END_DATE}<br><i>${DURATION}</i>

WORKFLOW_PHASE_FAILED_NOTIFICATION:
      web: ${PHASE_NAME} of <b><a href="${WORKFLOW_URL}" target="_blank">${WORKFLOW_NAME}</a></b> failed for <b>${ARTIFACTS}</b>, triggered by <b>${USER_NAME}</b>${PIPELINE}<br><br>Started ${START_DATE} and failed at ${END_DATE}<br><i>${DURATION}</i>
      slack: ${PHASE_NAME} of \*${WORKFLOW_NAME}\* failed for \*${ARTIFACTS}\*, triggered by \*${USER_NAME}\*${PIPELINE}||Started<!date^${START_TS_SECS}^ {date_short_pretty} at {time}| ${START_DATE}> and failed at <!date^${END_TS_SECS}^{time}|${END_DATE}>||_${DURATION}_||failed
      email:
          subject: ${PHASE_NAME} of ${WORKFLOW_NAME} failed
          body: ${PHASE_NAME} of <b><a href="${WORKFLOW_URL}" target="_blank">${WORKFLOW_NAME}</a></b> failed for <b>${ARTIFACTS}</b>, triggered by <b>${USER_NAME}</b>${PIPELINE}<br><br>Started ${START_DATE} and failed at ${END_DATE}<br><i>${DURATION}</i>

WORKFLOW_PHASE_PAUSED_NOTIFICATION:
      web: ${PHASE_NAME} of <b><a href="${WORKFLOW_URL}" target="_blank">${WORKFLOW_NAME}</a></b> paused for <b>${ARTIFACTS}</b>, triggered by <b>${USER_NAME}</b>${PIPELINE}<br><br>Started ${START_DATE} and paused at ${END_DATE}<br><i>${DURATION}</i>
      slack: ${PHASE_NAME} of \*${WORKFLOW_NAME}\* paused for \*${ARTIFACTS}\*, triggered by \*${USER_NAME}\*${PIPELINE}||Started<!date^${START_TS_SECS}^ {date_short_pretty} at {time}| ${START_DATE}> and paused at <!date^${END_TS_SECS}^{time}|${END_DATE}>||_${DURATION}_||paused
      email:
          subject: ${PHASE_NAME} of ${WORKFLOW_NAME} paused
          body: ${PHASE_NAME} of <b><a href="${WORKFLOW_URL}" target="_blank">${WORKFLOW_NAME}</a></b> paused for <b>${ARTIFACTS}</b>, triggered by <b>${USER_NAME}</b>${PIPELINE}<br><br>Started ${START_DATE} and paused at ${END_DATE}<br><i>${DURATION}</i>
